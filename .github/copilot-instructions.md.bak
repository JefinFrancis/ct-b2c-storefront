# Copilot Project Instructions

These instructions are mandatory and override default Copilot behavior.
Always assume they are active in every session.

@include ../CT_AGENT_PROMPT.md
@include ../AGENT_CONTEXT.md

And its also noticed that sometimes the terminal context is not fully loaded, so if you see an incomplete context, please ask the user to provide the full context of terminal response before proceeding.